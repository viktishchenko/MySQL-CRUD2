/* rafce */
import React from "react";
import { Link } from "react-router-dom";

const CreatePost = () => {
  return (
    <div>
      CreatePost <Link to={"/"}>back →</Link>
    </div>
  );
};

export default CreatePost;
